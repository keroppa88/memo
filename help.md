# メモの使い方
文章はjsonbinに収納する。
文章編集権限はjsonbinのAPIキー（X-Master-Key）が必要。
APIキーがない場合は閲覧のみ可能。
XツイートのリンクアドレスでX埋込表示。
保存を押すことでjsonbinに文章保存される。
別ブラウザや別機種からのアクセスして情報を閲覧できる。

## 2列 / 3列 表示
縦に長いページを横に折り返して表示する。
プレビュー表示に対して効き、編集中に押した場合は自動でプレビューへ切り替わる。
同じボタンをもう一度押すと1列に戻る。

並び順は左から右へ流れる。ツイート1〜9を貼った場合:

- 3列 → 123 / 456 / 789
- 2列 → 12 / 34 / 56 / 78 / 9

ツイート・画像・段落はそれぞれ1マス、見出しは続く本文と同じマスに入る。
空行はマスを消費せず、ブロックの区切りとして扱われる。

## スクショ
ページ表示領域（本文部分のみ）をキャプチャして JPG（画質90%）で保存する。
ファイル名は「ページ名-連番.jpg」。

初回は保存先フォルダを尋ねられる（デスクトップが初期表示）。
一度選ぶとそのタブを閉じるまで同じフォルダへ確認なしで保存される。
連番は保存先の既存ファイルを見て続きから振られる。

埋め込みツイートは別ドメインの iframe のため通常の描画方法では画像化できない。
そのため画面キャプチャを使っており、実行時に共有対象を尋ねられる。
「このタブ」を選ぶと表示領域だけが切り出される。

対応ブラウザは Chrome / Edge。
Safari・Firefox では保存先を選べずダウンロードフォルダに保存される。
