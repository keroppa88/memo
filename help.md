# メモの使い方
文章はjsonbinに収納する。
文章編集権限はjsonbinのAPIキー（X-Master-Key）が必要。
APIキーがない場合は閲覧のみ可能。
XツイートのリンクアドレスでX埋込表示。
保存を押すことでjsonbinに文章保存される。
別ブラウザや別機種からのアクセスして情報を閲覧できる。

## 2列 / 3列 表示
縦に長いページを横に折り返して表示する。
プレビュー表示に対して効き、編集中に押した場合は自動でプレビューへ切り替わる。
同じボタンをもう一度押すと1列に戻る。

並び順は左から右へ流れる。ツイート1〜9を貼った場合:

- 3列 → 123 / 456 / 789
- 2列 → 12 / 34 / 56 / 78 / 9

ツイート・画像・段落はそれぞれ1マス、見出しは続く本文と同じマスに入る。
空行はマスを消費せず、ブロックの区切りとして扱われる。

## スクショ
ボタン、または **Shift+P** で撮影する。
ページ表示領域（本文部分のみ）をキャプチャして JPG（画質90%）で保存する。
保存先はデスクトップの **memophoto** フォルダ。
ファイル名は「ページ名 + 年月日 + 時分秒.jpg」。

    Xトレード20260801202245.jpg

文字入力中は Shift+P が効かない（大文字のPを打てるようにするため）。
プレビュー表示中に使う想定。

### 許可について
ダイアログは最初の1回だけで、以降は押した瞬間に保存される。

- **キャプチャの許可**（「このタブの表示を許可しますか？」）
  一度許可するとその映像を使い回すので、2枚目以降は出ない。
  ブラウザの「共有を停止」を押した場合だけ次回また聞かれる。
- **保存先フォルダ**
  初回だけデスクトップを選ぶと、その下に memophoto フォルダを自動作成する。
  選んだ場所は記憶されるので、次に開いたときも選び直す必要はない。
  （タブを開き直した直後だけ、ブラウザがワンクリックの確認を出すことがある）

ファイル名に日時が入るので、別の日に撮り足しても前の画像を上書きしない。

### 制限
埋め込みツイートは別ドメインの iframe のため通常の描画方法では画像化できない。
そのため画面キャプチャを使っており、保存されるのは画面に見えている範囲になる。
共有対象に「このタブ」以外を選んだ場合は切り抜かずに全体を保存する。

対応ブラウザは Chrome / Edge。
Safari・Firefox ではフォルダ保存に対応していないためダウンロードフォルダに入る。
